""" 
Inference pipeline for EcoVision. 

This module loads the trained object detection model, runs prediction on input images, 
filters detections by confidence threshold, 
and converts raw model outputs into a structured format used by the web application. 
"""
